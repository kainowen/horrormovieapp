import React from 'react';
import Tag from "./Tag/Tag";

const Tags = (props) => {
    this.props.tags.map((tagname, index) => {
        return (
            <Tag

            />
        )
    })
};

export default Tags;